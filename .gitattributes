# Normalize line endings: treat all text as LF in the repo
* text=auto eol=lf

# Explicit text types
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.md   text eol=lf
*.json text eol=lf

# Binary assets — never touch
*.png binary
